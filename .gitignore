v-env
.env
!.env.example

node_modules
__pycache__
v-env
.DS_Store
aws_cf_deploy_anything_llm.json
yarn.lock
*.bak
.idea

# vs-fork: test-side-effect cache (Plan 1.5 v1.2.1)
server/__tests__/**/models/

# vs-fork: production deploy symlink — server/public points at
# frontend/dist so the express server can serve the SPA's built
# assets directly. Created post-build by the operator; never
# committed.
server/public
